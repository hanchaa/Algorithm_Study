#include <stdio.h>

int data[100000];

int main() {
	int n, m;
	long long lo = 0, hi = 0;

	scanf("%d %d", &n, &m);

	for (int i = 0; i < n; i++)
		scanf("%d", data + i);

	while (lo + 1 < hi) {

	}
}